[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (also in public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
